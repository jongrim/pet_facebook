{{> nav}}
<div class="container mt-5">
    <div class="row">
        <div class="col"></div>
        <div class="col-9">
            <h1><span class="fa fa-user"></span> Update Profile</h1>
            <div class="col s12 m6">
                {{#each User}}
                <form action="/updateProfile?_method=PUT" method="POST">
                <div class="form-group">
                    <label for="first_name">First Name</label>
                    <input type="first_name" value={{this.first_name}} class="form-control" name="first_name">
                </div>
                <div class="form-group">
                    <label for="last_name">Last Name</label>
                    <input type=last_name value={{this.last_name}} class="form-control" name=last_name>
                </div>
                <div class="form-group">
                    <label for="email">Email</label>
                    <input type="email" value={{this.email}} class="form-control" name="email">
                </div>
                {{!-- !!!!!This needs to be done on a seperate page using the passport change password options --}}
                {{!-- <div class="form-group">
                    <label for="password">Password</label>
                    <input type="password" value={{this.password}} class="form-control" name="password">
                </div>
                <div class="form-group">
                    <label for="confirm">Confirm password</label>
                    <input type="password" class="form-control" name="confirm">
                </div> --}}
                <label for="aboutMe">About Me:</label>
                <textarea type="aboutMe" name="aboutMe" placeholder="Write something about yourself" class="form-control" rows="6" id="aboutMe"></textarea>
                <br />
                <button type="submit" name="button" value="submit" class="btn btn-warning submit btn-sm">Update Profile</button>
                </form>
                {{/each}}
            </div>
            <hr>
            <p>Or go <a href="/">home</a>.</p>
        </div>
        <div class="col"></div>
    </div>
</div>